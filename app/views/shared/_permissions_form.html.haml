%div{ :class => defined?(p1) ? p1 : '33 a'}
  = form.input :permission, :collection => Lead.permissions, :include_blank => false

%div{ :class => defined?(p2) ? p2 : '33' }
  = form.input :permitted_user_ids, :collection => User.all.map {|u| [u.email, u.id] }, :include_blank => true
